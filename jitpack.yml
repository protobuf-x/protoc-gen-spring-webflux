jdk:
  - openjdk8
install:
  - ./gradlew clean -Pgroup=com.github.protobuf-x -Pversion=$VERSION -xtest assemble publishToMavenLocal
  - ls -la plugin/build/libs/
  - ls -la ~/.m2/repository/com/github/protobuf-x/protoc-gen-spring-webflux/$VERSION/